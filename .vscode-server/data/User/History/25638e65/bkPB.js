let employee = {
    name: denno,
    streetAddress: 483
};
employee("name", "streetAddress");

function updateObjectWithKeyAndValue(employee, denno, streetAddress) {
    return Object.assign({}, object, { [key]: value });
}