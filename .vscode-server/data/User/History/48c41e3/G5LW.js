let message = [];
function writecards(names, eventName) {
    let n = names.length;
    for (let i = 0; i < n; i++) {

        message = ` "Thank you, ${names[i]}for the wonderful ${eventname} gift!" `;
    }
    return message;
}
