// Code your solutions in this file
const cards = ["kero", "tugi ", "ngae"];

function writecards(cards) {
    for (let i = 0; i < cards.length; i++) {
        console.log(`thank you  ${cards[i]} for the wonderfull birthday gift`);

    }

    return writecards;
}

writecards(cards);
